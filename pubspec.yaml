name: barakah90
description: A new Flutter project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # إصدارات متوافقة ومستقرة لخدمات Firebase
  firebase_core: ^2.32.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.17.5

  shared_preferences: ^2.2.3
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # تأكد من وجود هذه المجلدات فعليًا في مشروعك
  assets:
    - assets/images/categories/
    - assets/images/backgrounds/
    - assets/images/restaurants/
